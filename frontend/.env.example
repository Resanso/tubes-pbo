# Copy this file to .env.local and fill in your values.
# Never commit .env.local to git.

# In development the Vite proxy handles /api → http://localhost:8080
# Override this only for a deployed frontend pointing at a remote backend
VITE_API_BASE_URL=/api
