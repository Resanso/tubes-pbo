import React from 'react';
import type { SavingsPlan } from '@/types/decision.types';

interface Props {
  plan: SavingsPlan;
}

const SavingsPlanCard: React.FC<Props> = (_props) => {
  // TODO: To be implemented by team
  return <div />;
};

export default SavingsPlanCard;
